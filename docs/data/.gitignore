# Data directory - contains generated files
# These are created by unified_fetcher.py during CI/CD

# Keep the directory structure but ignore contents
crash-detector/*.json
ai-race/*.json
economic-compass/*.json
hyper-analytical/*.json
intelligence-platform/*.json
free-knowledge/*.json
dashboard-orchestrator/*.json

# Keep cache directory but exclude contents
cache/

# Except for .gitkeep files
!.gitkeep
